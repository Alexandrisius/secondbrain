# 🔧 Инструкция по сборке NeuroCanvas

## Подготовка к сборке

### 1. Подготовка иконки

Перед сборкой необходимо создать иконки приложения:

#### Вариант A: Онлайн конвертация (рекомендуется)

1. Откройте файл `electron/icon.svg` в браузере
2. Сделайте скриншот или экспортируйте в PNG (1024x1024 px)
3. Конвертируйте через онлайн сервисы:
   - Для Windows (.ico): https://cloudconvert.com/png-to-ico
   - Для macOS (.icns): https://cloudconvert.com/png-to-icns
4. Сохраните файлы:
   - `electron/icon.ico` (256x256 или 512x512)
   - `electron/icon.icns` (512x512)

#### Вариант B: Через electron-icon-builder

```bash
# Создайте PNG файл 1024x1024 из SVG (через Figma, Photoshop или онлайн)
# Сохраните как electron/icon.png

# Запустите генерацию иконок
npx electron-icon-builder --input=electron/icon.png --output=electron/icons
```

### 2. Настройка ссылок на донаты

Откройте файл `src/components/canvas/DonateModal.tsx` и замените placeholder'ы:

```typescript
const DONATE_LINKS = {
  boosty: 'https://boosty.to/YOUR_BOOSTY_USERNAME',  // ← Ваш username
  kofi: 'https://ko-fi.com/YOUR_KOFI_USERNAME',      // ← Ваш username  
  github: 'https://github.com/YOUR_USERNAME/neurocanvas', // ← Ваш репозиторий
};
```

Также обновите ссылки в `electron/main.js` в меню "Помощь".

## Сборка приложения

### Для разработки (dev режим)

```bash
# Запуск Electron с hot-reload
npm run electron:dev
```

Приложение запустится и будет автоматически перезагружаться при изменениях.

### Production сборка

#### Windows

```bash
npm run electron:build:win
```

Результат будет в папке `dist/`:
- `NeuroCanvas-Setup-1.0.0.exe` — установщик
- `NeuroCanvas-1.0.0-portable.exe` — портативная версия

#### macOS

```bash
npm run electron:build:mac
```

Результат:
- `NeuroCanvas-1.0.0.dmg` — установочный образ
- `NeuroCanvas-1.0.0-mac.zip` — архив приложения

#### Все платформы

```bash
npm run electron:build:all
```

⚠️ **Примечание:** Сборка для macOS на Windows возможна только через CI/CD (GitHub Actions).

## Структура dist/

После сборки структура папки `dist/`:

```
dist/
├── win-unpacked/           # Распакованная Windows версия
├── mac/                    # macOS приложение
├── NeuroCanvas-Setup-1.0.0.exe    # Windows установщик
├── NeuroCanvas-1.0.0-portable.exe # Windows portable
├── NeuroCanvas-1.0.0.dmg          # macOS установщик
└── NeuroCanvas-1.0.0-mac.zip      # macOS архив
```

## Автообновления (опционально)

Для включения автообновлений через GitHub Releases:

1. Раскомментируйте секцию `publish` в `electron-builder.yml`
2. Создайте GitHub Release с собранными файлами
3. Приложение будет автоматически проверять обновления

## Публикация

### Бесплатное распространение

1. Создайте релиз на GitHub
2. Загрузите .exe и .dmg файлы
3. Поделитесь ссылкой!

### Платформы для распространения

- **GitHub Releases** — бесплатно, с автообновлениями
- **Itch.io** — для indie-приложений
- **Microsoft Store** — требует подписи (платно)
- **Mac App Store** — требует Apple Developer аккаунт ($99/год)

## Troubleshooting

### Ошибка "Cannot find module 'electron'"

```bash
npm install
npm run postinstall
```

### Ошибка сборки на Windows

Убедитесь что установлены:
- Visual Studio Build Tools
- Python (для node-gyp)

```bash
npm install --global windows-build-tools
```

### Большой размер сборки

Electron включает Chromium (~100MB). Это нормально.
Для уменьшения размера используйте Tauri (альтернатива).

---

Сделано с ❤️ для NeuroCanvas

